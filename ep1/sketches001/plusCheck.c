#include <stdio.h>

int main () {
    int a, b, c;	/* three normal variables */

    a = 2, b = 3;	/* a = 2, b = 3 */
    c = a + b;		/* c = a + b */

    if (c > 5)  printf("higher");	/* if(c>5) */
    else	printf("here we are");	/* else */

    return 0;
}
